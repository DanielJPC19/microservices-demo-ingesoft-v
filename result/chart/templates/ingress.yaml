apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: result
  annotations: {}
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: result
                port:
                  number: 80
